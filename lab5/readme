Совместно с Егором Панковым.
По умолчанию в settings уже указаны пути к нужным файлам, но их можно менять.
1. G_VERBOSE - режим отображения процесса обработки словаря
2. OUTPUT_TO_FILE - режим вывода в файл (если False то на экран)
3. OUTPUT_FILE - файл куда осуществляется вывод в режиме вывода в файл
4. DEFAULT_G_FILE - по умолчанию программа использует словарь из этого файла, если он находится в корне
5. DEFAULT_W_FILE - по умолчанию программа использует выражение из этого файла, если он находится в корне 
6. G_FILE - путь к файлу со словарем, если файл по умолчанию не задан 
7. W_FILE - путь к файлу с выражением, если файл по умолчанию не задан
